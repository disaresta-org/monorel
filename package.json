{
  "name": "monorel-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-level tooling for monorel: a Conventional-Commits message linter for git hooks and CI. The docs site has its own package.json under docs/.",
  "type": "module",
  "scripts": {
    "lint-commits": "node scripts/lint-commit.mjs"
  },
  "devDependencies": {
    "@conventional-commits/parser": "^0.4.1"
  }
}
