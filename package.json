{
  "name": "monorel-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-level tooling for monorel: a commit-message linter that uses the same parser monorel-style projects use, plus a doctoc TOC regenerator. The docs site has its own package.json under docs/.",
  "type": "module",
  "scripts": {
    "lint-commits": "node scripts/lint-commit.mjs",
    "toc": "doctoc README.md --github --title '## Table of contents'"
  },
  "devDependencies": {
    "@conventional-commits/parser": "^0.4.1",
    "doctoc": "^2.2.1"
  }
}
